# Textured 3D preview site

Отвори `index.html` през static server или deploy-ни папката.

Локален тест:

```bash
cd SITE_FOLDER
python3 -m http.server 8787
```

После отвори `http://localhost:8787`.

Това е 3D trap-catcher, не финално beauty proof.
